# DNS — Local Name Resolution

Turn IP addresses into human-readable names.

!!! info "Work in Progress"
    This guide will cover setting up a local DNS server for your community network.

## What You'll Learn

- DNS fundamentals (A records, CNAME, PTR)
- Setting up dnsmasq on OpenWrt for basic local DNS
- Running a dedicated DNS server (Pi-hole, AdGuard Home, or Bind)
- Choosing a local domain naming convention
- Split-horizon DNS (different answers inside vs outside the network)

<!-- TODO: dnsmasq configuration, Pi-hole setup, naming conventions, DNS debugging tools -->
